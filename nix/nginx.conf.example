# Include in the existing HTTPS server; host names and certificates belong to infrastructure.
location / {
    proxy_pass http://127.0.0.1:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto https;
    client_max_body_size 2m;
}
location /internal/ { return 404; }

# Put the following in a separate HTTPS server restricted to the executor network.
# Require a host-managed client certificate (ssl_verify_client on) and trusted CA.
# location /internal/ {
#     proxy_pass http://127.0.0.1:8081;
#     client_max_body_size 8m;
#     access_log off;
# }
